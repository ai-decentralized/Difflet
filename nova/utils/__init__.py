"""Hardware-neutral utilities.

Trainium runtime helpers live under ``nova.backends.trainium.utils``. The
legacy ``compile_env``, ``runtime_env``, ``distributed``, and ``snapshot``
modules in this package are compatibility shims only.
"""
